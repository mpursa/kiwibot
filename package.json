{
	"name": "kiwibot",
	"version": "1.5.2",
	"description": "Discord bot for starting and stopping game servers via systemd",
	"type": "module",
	"author": {
		"name": "mpursa",
		"email": "m.pretto93@gmail.com",
		"url": "https://github.com/mpursa"
	},
	"license": "BSD-2-Clause",
	"engines": {
		"node": ">=20.6"
	},
	"scripts": {
		"build": "tsc",
		"clean": "node -e \"['dist', 'dist-tests'].forEach((d) => fs.rmSync(d, { recursive: true, force: true }))\"",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"rebuild": "npm run clean && npm run build",
		"watch": "tsc --watch",
		"start": "node --env-file=.env dist/main.js",
		"test": "npm run rebuild && tsc -p tests && node --env-file=.env.test --test dist-tests/",
		"testCommand": "npm run build && tsc -p tests && node --env-file=.env.test dist-tests/run-command.js",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"discord.js": "^14.16.0"
	},
	"devDependencies": {
		"@types/node": "^22.7.0",
		"prettier": "^3.9.6",
		"typescript": "^7.0.2"
	}
}
